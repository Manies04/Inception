[www]
user = www-data
group = www-data

; Escuta na porta 9000 em vez de socket
; Assim o NGINX consegue ligar-se
listen = 0.0.0.0:9000

pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3